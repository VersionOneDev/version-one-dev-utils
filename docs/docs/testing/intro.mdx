---
sidebar_position: 1
---

# Intro

We can use Reacts default test runner, @testing-library/react, to test storybook stories which use the state and routing utils.

The dev utils also also expose a TestId helper function that can be used across your tests (not just stories of components).
